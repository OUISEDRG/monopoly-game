# 清理归档记录

## 基本信息

- **记录编号**：ARCHIVE-20260611-0540
- **生成时间**：2026-06-11 05:40（Asia/Shanghai）
- **执行者**：Codex（项目大脑 / 协调审查者）
- **任务名称**：审查公网联机实施计划 Task 2
- **任务状态**：审查完成，Task 2 需要返修

## 保留文件

| 文件 | 原因 |
|---|---|
| `server/models/player.py` | Task 2 玩家模型，待修正默认起始资金 |
| `server/models/game.py` | Task 2 游戏状态模型，当前检查未发现阻塞问题 |
| `server/models/room.py` | Task 2 房间模型和快照，待修正私密字段边界及信封结构 |
| `server/models/__init__.py` | models 包入口 |
| `tests/engine/test_state_models.py` | Task 2 测试，待补充契约回归覆盖 |

## 归档文件

| 原位置或来源 | 最终位置 | 说明 |
|---|---|---|
| 本轮 Task 2 审查结果 | `docs/superpowers/archives/2026-06-11-0540-Codex-清理归档记录.md` | 记录验证证据与返修原因 |

## 删除文件

| 文件 | 删除理由 | 可重建方式 |
|---|---|---|
| 仓库内全部 `__pycache__/` 与 `*.pyc` | pytest、导入和编译生成的可重建缓存 | 重新运行 Python 或测试 |

## 受保护文件

| 文件 | 保护理由 |
|---|---|
| `monopoly.html`、规则与评审文档 | 前序项目改动，也是核对起始资金的事实来源 |
| Task 0/1 实现及现有 Node 测试 | 已验收的持久项目文件 |
| 提交 `6734b0e` 所含内容 | 已存在 Git 提交；本轮不擅自回退或改写历史 |

## 验证证据

| 检查 | 结果 | 备注 |
|---|---|---|
| `python -m pytest tests/engine/test_state_models.py -q` | 通过 | 36 passed，但缺少三项关键契约测试 |
| `python -m pytest -q` | 通过 | 54 passed，1 条既有 TestClient 弃用警告 |
| `node --test tests/*.mjs` | 通过 | 46 passed，0 failed |
| Python 编译 | 通过 | Task 2 模型、服务入口和单机入口无语法错误 |
| `git diff --check` | 通过 | 无空白错误 |
| `RoomState` 字段检查 | 不通过 | 包含设计未允许的 `_reconnect_token_hashes` |
| 起始资金事实核对 | 不通过 | 模型为 0；旧版游戏和规则均为 15000 |
| 快照协议形状核对 | 不通过 | `state` 内重复信封元数据 |
| 临时缓存复查 | 通过 | 未剩余 `__pycache__` |

## 剩余事项

- Task 2 需要返修后再次由 Codex 独立验收，当前不得进入 Task 3。
- 当前 HEAD 为 `6734b0e`，提交时间早于 Task 2 文件创建时间；Task 2 文件仍是未跟踪状态。无法仅凭仓库证明该提交由谁发起，因此只记录时间线，不做归责或回退。
