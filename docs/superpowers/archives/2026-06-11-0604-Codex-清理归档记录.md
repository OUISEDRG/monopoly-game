# 清理归档记录

## 基本信息

- **记录编号**：ARCHIVE-20260611-0604
- **生成时间**：2026-06-11 06:04（Asia/Shanghai）
- **执行者**：Codex（项目大脑 / 协调审查者）
- **任务名称**：审查公网联机实施计划 Task 3
- **任务状态**：审查完成，Task 3 需要返修

## 保留文件

| 文件 | 原因 |
|---|---|
| `server/protocol.py` | Task 3 协议实现，待收紧类型与函数签名 |
| `tests/server/test_protocol.py` | Task 3 测试，待补充严格类型和公开签名覆盖 |
| `server/models/`、`tests/engine/test_state_models.py` | 已验收的 Task 2 文件 |

## 归档文件

| 原位置或来源 | 最终位置 | 说明 |
|---|---|---|
| 本轮 Task 3 审查结果 | `docs/superpowers/archives/2026-06-11-0604-Codex-清理归档记录.md` | 验证证据与返修原因 |

## 删除文件

| 文件 | 删除理由 | 可重建方式 |
|---|---|---|
| 仓库内全部 `__pycache__/` 与 `*.pyc` | pytest、导入和编译生成的可重建缓存 | 重新运行 Python 或测试 |

## 受保护文件

| 文件 | 保护理由 |
|---|---|
| `monopoly.html`、规则和评审文档 | 前序项目改动 |
| Task 0–2 源码和既有测试 | 已验收持久文件 |
| Git 提交 `6734b0e` | 既有提交，本轮未创建或改写 |

## 验证证据

| 检查 | 结果 | 备注 |
|---|---|---|
| `python -m pytest tests/server/test_protocol.py -q` | 通过 | 28 passed，但缺少严格类型和关键字签名测试 |
| `python -m pytest -q` | 通过 | 85 passed，1 条既有 TestClient 弃用警告 |
| `node --test tests/*.mjs` | 通过 | 46 passed，0 failed |
| Python 编译 | 通过 | 无语法错误 |
| `git diff --check` | 通过 | 无空白错误 |
| roomVersion 边界探针 | 不通过 | `"1"`、`1.0`、`true` 均被接受并转换成整数 1 |
| `rejected_result(code=...)` 探针 | 不通过 | 参数名为 `error_code`，按计划关键字调用产生 `TypeError` |
| UTF-8 16 KiB 探针 | 通过 | 16383 和 16384 字节均可解析，超过上限逻辑已由测试覆盖 |
| payload 隔离探针 | 通过 | 两个模型实例不共享默认字典 |
| 临时缓存复查 | 通过 | 未剩余 `__pycache__` |

## 剩余事项

- Task 3 返修后必须再次由 Codex 验收，当前不得进入 Task 4。
- 当前未知命令分类依赖 Pydantic 英文错误文本匹配，需改为基于输入值或结构化错误信息的稳定分类。
