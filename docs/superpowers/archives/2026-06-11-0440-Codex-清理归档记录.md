# 清理归档记录

## 基本信息

- **记录编号**：ARCHIVE-20260611-0440
- **生成时间**：2026-06-11 04:40（Asia/Shanghai）
- **执行者**：Codex
- **任务名称**：审查公网联机实施计划 Task 0
- **任务状态**：完成

## 保留文件

| 文件 | 原因 |
|---|---|
| `requirements.txt` | Task 0 的 Python 依赖声明 |
| `pytest.ini` | Task 0 的 pytest 配置 |
| `server/__init__.py` | Task 0 的服务端包入口 |
| `server/app.py` | Task 0 的最小 FastAPI 应用与 `/healthz` |
| `tests/server/test_health.py` | Task 0 的健康检查测试 |
| `docs/superpowers/plans/2026-06-11-online-multiplayer-implementation.md` | 后续任务的正式实施计划 |
| `docs/superpowers/specs/2026-06-11-online-multiplayer-design.md` | 联机系统正式设计规范 |

## 归档文件

| 原位置或来源 | 最终位置 | 说明 |
|---|---|---|
| 本轮 Task 0 审查结果 | `docs/superpowers/archives/2026-06-11-0440-Codex-清理归档记录.md` | 本轮清理与验证证据 |
| Task 1 计划勘误 | `docs/superpowers/plans/2026-06-11-online-multiplayer-implementation.md` | 将起点类型示例由错误的 `"start"` 改为旧版真实值 `"go"` |

## 删除文件

| 文件 | 删除理由 | 可重建方式 |
|---|---|---|
| 仓库内全部 `__pycache__/` 与其中 `*.pyc` | 本轮 pytest 与 `py_compile` 产生的 Python 缓存，不属于源码 | 重新运行 Python、pytest 或 `py_compile` |

## 受保护文件

| 文件 | 保护理由 |
|---|---|
| `monopoly.html` | 前序未提交的游戏实现改动 |
| `monopoly_review.md` | 前序未提交的审查文档改动 |
| `桌游版大富翁经济系统与规则.md` | 前序未提交的规则文档改动 |
| `.agents/`、`.gitignore`、`README.md`、既有 `docs/` 与 `tests/` 内容 | 用户或前序 AI 的未提交持久文件，未做删除或覆盖 |

## 验证证据

| 检查 | 结果 | 备注 |
|---|---|---|
| `python -m pytest tests/server/test_health.py -q` | 通过 | 1 passed，另有 1 条 FastAPI TestClient 弃用警告 |
| `node --test tests/*.mjs` | 通过 | 46 passed，0 failed |
| `python -m py_compile server/app.py monopoly_app.py` | 通过 | 无编译错误 |
| `git diff --check` | 通过 | 仅报告既有文件 LF/CRLF 提示 |
| 临时缓存复查 | 通过 | 仓库内未剩余 `__pycache__` |

## 剩余事项

- 工作区仍包含大量前序未提交改动，均按保护规则保留。
- FastAPI TestClient 的弃用警告不影响 Task 0 验收；后续依赖升级时再集中处理，不在本轮扩展范围。
- Task 1 计划示例中的起点类型 `"start"` 与旧版真实值 `"go"` 不一致；已在新交接任务包中明确要求以旧版 `"go"` 为准。
